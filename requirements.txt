# Pinned with `~=` (compatible release: patch updates only) rather than left
# open. The file was fully unpinned, so `pip install -r requirements.txt` could
# resolve to a different Flask/anthropic/gspread on every machine and every
# rebuild — and this project's correctness rules (SQLite row factories, the
# httpx verify=False client, gspread's get_all_values shape) are version-
# sensitive. Versions below are the ones the suite is green against.
flask~=3.1.3
python-dotenv~=1.2.3
gspread~=6.2.1
google-auth~=2.58.0
slack_sdk~=3.44.1
anthropic~=1.6.0
httpx~=0.28.1

# Tests: `python3 -m pytest` from the repo root (config in pytest.ini).
pytest~=9.1.1
